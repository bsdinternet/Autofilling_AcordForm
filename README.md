# Autofilling_AcordForm
These scripts form a PDF form extraction and filling pipeline specifically for ACORD insurance forms:  acord_extract.py – Extracts form fields from an AcroForm PDF and generates two JSON files (full metadata + slim text-only) pdfwriter.py – Fills an AcroForm PDF with data from the extracted JSON files,
